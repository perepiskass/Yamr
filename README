1) Запуск:
    yamr <имя файла для считывания данных> <колличество потоков для map функции> <колличество потоков для reduce функции>
2) Результат:
    На консоль выводиться минимальное число символов для определения уникальности строки из заданного источника
    Так же создаються файлы, по колличеству потоков для функции reduce (если потоков больше чем исходных данных, то по колличеству исходных данных)
    В файле отражена подстрока однозначно идентифицирующая строку и колличество таких подстрок(везде будет 1)
    
    Если в исходных данных была задублирована строка,то на консоль выводиться сообщение об этом.
    Минимальная длина строки будет размером с самую длинную строку в исходном файле.
    Файлы будут содержать полные строки, с их колличеством в исходном файле.